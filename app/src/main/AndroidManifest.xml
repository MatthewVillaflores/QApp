<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tech.ivant.qapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewService"
            android:label="@string/title_activity_new_service" >
        </activity>
        <activity
            android:name=".ViewService"
            android:label="@string/title_activity_view_service" >
        </activity>
        <activity
            android:name=".NewQueue"
            android:label="@string/title_activity_new_queue" >
        </activity>
        <activity
            android:name=".callNext"
            android:label="@string/title_activity_call_next"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.tech.ivant.qapp.MainActivity" />
        </activity>
    </application>

</manifest>
