<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ListView
        android:layout_width="match_parent"
        android:layout_height="247dp"
        android:id="@+id/queueListView"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/queueControlButtons"
        android:orientation="horizontal"
        android:gravity="center_vertical|center_horizontal"
        android:weightSum="1">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/view_service_queue_button"
            android:id="@+id/addQueueButton"
            android:background="@drawable/roundedbutton"
            android:textColor="@android:color/white"
            android:textAppearance="@android:style/TextAppearance.Small"
            android:drawableLeft="@drawable/plusbtn_orange"

            android:layout_gravity="left|top"
            android:layout_weight="0.03" />

        <Button
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:text="@string/view_service_call_next"
            android:id="@+id/callNextButton"
            android:layout_gravity="right|top"
            android:background="@drawable/roundedbutton"
            android:textColor="@android:color/white"
            android:textAppearance="@android:style/TextAppearance.Small"
            android:drawableLeft="@drawable/bellbtn_orange"
            android:layout_marginLeft="10dp"

            android:layout_weight="0.03">

        </Button>
    </LinearLayout>

</LinearLayout>